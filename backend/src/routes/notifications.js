// Notification emails are handled by the Supabase Edge Function `send-notification`.
// This file is kept as a stub in case direct API calls are needed in future.

import { Router } from 'express'
const router = Router()
export default router
